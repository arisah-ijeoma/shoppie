$('#category_products').html("<%= j(render 'filter') %>")

<% link_toggle = params[:category].blank? ? '' : (link_to 'Clear filter', category_path(@category),
class: 'clear-filter', remote: true) %>

$('#clear_filter').html("<%= j(link_toggle) %>")
